# 统一 RAG 技术架构方案

## 1. 方案概述

### 1.1 业务场景

本方案旨在构建一个统一的技术架构，支持以下四个业务场景：

| 场景 | 类型 | 核心能力 |
|------|------|----------|
| 风控规则问答 | RAG 检索 | 从规则文档库中检索相关规则，结合 LLM 生成答案 |
| 模型卡片检索 | RAG 检索 | 从模型卡片库中检索匹配的模型，返回详细信息 |
| 仿真结果解读 | Tool 调用 | 直接调用仿真系统 API，获取结果并由 LLM 解读 |
| 毛利抽成查询 | Tool 调用 | 直接查询数据库/API，返回计算结果 |

### 1.2 设计原则

- **统一基础设施**：共用 Embedding 模型、向量数据库、LLM 服务
- **灵活路由**：根据场景自动选择 RAG 检索或 Tool 调用
- **可扩展性**：易于新增场景和数据源
- **高性能**：支持并发查询、缓存优化
- **可观测性**：完整的日志、监控、追踪体系

### 1.3 技术栈选型

#### 1.3.1 开发语言：Python

**选择 Python 的理由：**

| 维度 | Python | Java | 结论 |
|------|--------|------|------|
| RAG 生态 | LangChain/LangGraph 原生支持，文档丰富 | LangChain4j 生态不成熟 | ✅ Python |
| Embedding 模型 | BGE-M3、Sentence-Transformers 直接调用 | 需要通过 ONNX 或 API | ✅ Python |
| 开发效率 | 代码简洁，开发速度快 2-3 倍 | 代码冗长，类型声明多 | ✅ Python |
| AI/ML 生态 | PyTorch/TensorFlow 原生支持 | 需要通过 JNI 或 API | ✅ Python |
| 性能 | 相对较低（但瓶颈在 LLM API） | 高并发性能更好 | ⚖️ 影响不大 |
| 企业级特性 | 类型安全性弱 | 类型安全，维护性好 | ⚖️ 可用 Pydantic 缓解 |

**结论：** 选择 **Python** 作为主要开发语言，快速上线 MVP，后续如有性能瓶颈可考虑混合架构。

#### 1.3.2 编排框架：LangChain + LangGraph

**技术分工：**

| 组件 | 职责 | 使用场景 |
|------|------|----------|
| **LangChain** | 提供 RAG 基础组件 | Document Loaders、Text Splitters、Embeddings、Vector Stores、Retrievers、Prompts、LLMs |
| **LangGraph** | 构建复杂 Agent 工作流 | 意图识别、条件路由、状态管理、多轮对话 |

**为什么需要 LangGraph？**

本项目有**四个场景需要动态路由**，LangChain 只能实现线性流程（A → B → C），无法根据用户意图进行条件分支。LangGraph 提供：
- ✅ 条件路由（根据意图识别结果路由到不同场景）
- ✅ 状态管理（支持多轮对话）
- ✅ 可视化工作流
- ✅ 循环执行和人工介入

**架构示意：**
```
用户问题
    ↓
意图识别（LangGraph）
    ↓
  ┌─────┴─────┬─────────┬─────────┐
  ↓           ↓         ↓         ↓
风控规则    模型卡片   仿真解读   毛利查询
(LangChain) (LangChain) (Tool)   (Tool)
```

## 2. 技术架构

### 2.1 整体架构

```
┌─────────────────────────────────────────────────────────────┐
│                        用户请求层                              │
│                    (API Gateway / BFF)                       │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│                    意图识别与路由层                            │
│              (LangGraph Agent Orchestrator)                 │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐   │
│  │风控规则  │  │模型卡片  │  │仿真解读  │  │毛利查询  │   │
│  │  Agent   │  │  Agent   │  │  Agent   │  │  Agent   │   │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘   │
└─────────────────────────────────────────────────────────────┘
           ↓                ↓              ↓            ↓
    ┌──────────┐      ┌──────────┐   ┌──────────┐ ┌──────────┐
    │ RAG 检索 │      │ RAG 检索 │   │ Tool 调用│ │ Tool 调用│
    └──────────┘      └──────────┘   └──────────┘ └──────────┘
           ↓                ↓              ↓            ↓
┌─────────────────────────────────────────────────────────────┐
│                      基础设施层                               │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │ Embedding    │  │ Vector DB    │  │ LLM Service  │      │
│  │ (BGE-M3)     │  │ (Milvus)     │  │ (GPT-4/本地) │      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
└─────────────────────────────────────────────────────────────┘
           ↓                ↓              ↓
┌─────────────────────────────────────────────────────────────┐
│                      数据源层                                 │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐   │
│  │规则文档库│  │模型卡片库│  │仿真系统  │  │业务数据库│   │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘   │
└─────────────────────────────────────────────────────────────┘
```

### 2.2 核心技术选型

#### 2.2.0 上游系统调用方式

**方案对比：**

| 方案 | 性能 | 易用性 | 适用场景 | 推荐度 |
|------|------|--------|----------|--------|
| **HTTP REST API** | 中等 | ⭐⭐⭐⭐⭐ | 实时查询、外部调用、调试 | ⭐⭐⭐⭐⭐ |
| **gRPC** | 高（比 HTTP 快 5-10 倍） | ⭐⭐⭐ | 高并发、内部服务、流式输出 | ⭐⭐⭐⭐ |
| **消息队列** | 低（异步） | ⭐⭐⭐ | 批量处理、异步任务、削峰填谷 | ⭐⭐⭐ |

**推荐方案：HTTP REST API（主） + gRPC（可选）**

**HTTP REST API 实现：**

```python
# Python 端（FastAPI）
from fastapi import FastAPI
from pydantic import BaseModel

app = FastAPI()

class QueryRequest(BaseModel):
    query: str
    scene_type: str = None  # 可选，不传则自动识别
    user_id: str = None

class QueryResponse(BaseModel):
    answer: str
    sources: list
    scene_type: str
    request_id: str

@app.post("/api/v1/query")
async def query(request: QueryRequest) -> QueryResponse:
    result = await rag_pipeline.process(
        query=request.query,
        scene_type=request.scene_type,
        user_id=request.user_id
    )
    return QueryResponse(**result)
```

**Java 端调用：**

```java
@Service
public class RagService {
    @Autowired
    private RestTemplate restTemplate;
    
    private static final String RAG_API_URL = "http://rag-service:8000/api/v1/query";
    
    public QueryResponse query(String query, String sceneType) {
        QueryRequest request = new QueryRequest(query, sceneType);
        ResponseEntity<QueryResponse> response = restTemplate.postForEntity(
            RAG_API_URL, request, QueryResponse.class
        );
        return response.getBody();
    }
}
```

**gRPC 方案（高性能场景）：**

适用于：
- 高并发场景（QPS > 1000）
- 需要流式返回（LLM 逐字输出）
- 内部服务间调用

详细实现见附录 A。

#### 2.2.1 编排框架：LangChain + LangGraph

**选型理由：**
- **LangChain**：提供丰富的组件（Document Loaders, Text Splitters, Retrievers）
- **LangGraph**：支持复杂的 Agent 工作流，状态管理，条件路由
- **生态成熟**：与主流 LLM、向量数据库无缝集成

**使用场景：**
- 意图识别与路由
- RAG 检索链编排
- Tool 调用与结果处理
- 多轮对话状态管理

**LangChain vs LangGraph 分工：**

```python
# LangChain：处理单个场景的 RAG 逻辑
from langchain.chains import RetrievalQA
from langchain.vectorstores import Milvus

# 风控规则场景
risk_rule_chain = RetrievalQA.from_chain_type(
    llm=llm,
    retriever=vectorstore.as_retriever(
        search_kwargs={"expr": "scene_type == 'risk_rule'", "k": 5}
    )
)

# LangGraph：处理多场景路由和编排
from langgraph.graph import StateGraph

workflow = StateGraph(AgentState)
workflow.add_node("classify", classify_intent)  # 意图识别
workflow.add_node("risk_rule", handle_risk_rule)
workflow.add_node("model_card", handle_model_card)
workflow.add_node("simulation", handle_simulation)
workflow.add_node("profit", handle_profit)

# 条件路由
workflow.add_conditional_edges(
    "classify",
    lambda state: state["scene_type"],
    {
        "risk_rule": "risk_rule",
        "model_card": "model_card",
        "simulation": "simulation",
        "profit": "profit"
    }
)

app = workflow.compile()
```

**为什么不能只用 LangChain？**

LangChain 只支持线性流程（A → B → C），无法实现：
- ❌ 根据用户意图动态路由到不同场景
- ❌ 多轮对话的状态管理
- ❌ 复杂的条件分支和循环

LangGraph 基于状态图，可以实现：
- ✅ 条件路由（if-else 逻辑）
- ✅ 状态持久化（记忆上下文）
- ✅ 可视化工作流
- ✅ 人工介入（Human-in-the-loop）

#### 2.2.2 Embedding 模型：BGE-M3

**选型理由：**
- **多语言支持**：中英文效果优秀
- **混合检索**：同时支持稠密向量 + 稀疏向量（BM25-like）
- **性能优秀**：在 MTEB 排行榜上表现优异
- **部署灵活**：可本地部署，降低成本

**备选方案：**
- OpenAI `text-embedding-3-small`：API 调用简单，但成本较高
- 使用场景：如果对延迟要求极高，可考虑 API 方案

#### 2.2.3 向量数据库：Milvus（推荐）

**选型理由：**
- **混合检索**：支持稠密向量 + 稀疏向量 + 标量过滤
- **高性能**：百万级数据毫秒级响应
- **标量过滤**：支持按场景类型、时间范围等过滤
- **分区管理**：可按场景分区，提升检索效率
- **云原生**：支持 K8s 部署，易于扩展

**备选方案：Qdrant**
- 优势：部署更简单，内存占用更小
- 劣势：混合检索能力弱于 Milvus
- 适用场景：数据量 < 100 万，对混合检索要求不高

**Collection 设计：**
```python
# 统一 Collection，通过 metadata 区分场景
{
    "id": "doc_001",
    "vector": [0.1, 0.2, ...],  # 稠密向量
    "sparse_vector": {...},      # 稀疏向量（可选）
    "scene_type": "risk_rule",   # 场景类型
    "content": "...",            # 原始内容
    "metadata": {                # 场景特定元数据
        "rule_id": "R001",
        "category": "反欺诈",
        "update_time": "2026-05-01"
    }
}
```

#### 2.2.4 LLM 服务

**选型策略：**
- **主力模型**：GPT-4 Turbo / Claude 3.5 Sonnet（高质量场景）
- **辅助模型**：GPT-3.5 Turbo（简单查询、意图识别）
- **本地部署**：Qwen2.5-72B / Llama 3.1-70B（成本敏感场景）

**场景分配：**
| 场景 | 推荐模型 | 理由 |
|------|----------|------|
| 风控规则问答 | GPT-4 Turbo | 需要准确理解复杂规则逻辑 |
| 模型卡片检索 | GPT-3.5 Turbo | 主要是信息提取，不需要复杂推理 |
| 仿真结果解读 | GPT-4 Turbo | 需要深度分析数值结果 |
| 毛利抽成查询 | GPT-3.5 Turbo | 简单的数据查询和格式化 |

## 3. 四个场景的实现方案

### 3.1 场景一：风控规则问答（RAG）

**数据准备：**
1. 收集风控规则文档（PDF、Word、Markdown）
2. 使用 LangChain Document Loaders 加载
3. 文本分块（RecursiveCharacterTextSplitter，chunk_size=500, overlap=50）
4. 生成 Embedding 并存入 Milvus

**检索流程：**
```python
用户问题 
  → Query Embedding
  → Milvus 混合检索（稠密向量 + BM25 + 标量过滤）
  → Rerank（可选，使用 BGE-Reranker）
  → 构建 Prompt（问题 + Top-K 文档）
  → LLM 生成答案
  → 返回答案 + 引用来源
```

**优化策略：**
- **查询改写**：使用 LLM 将口语化问题改写为检索友好的查询
- **混合检索**：结合语义相似度（稠密向量）和关键词匹配（稀疏向量）
- **时间过滤**：优先返回最新版本的规则
- **引用溯源**：返回答案时附带原文档位置

### 3.2 场景二：模型卡片检索（RAG）

**数据准备：**
1. 模型卡片结构化数据（JSON/数据库）
2. 将每个模型卡片转换为文本描述
3. 生成 Embedding 并存入 Milvus

**检索流程：**
```python
用户查询（如："推荐一个适合信用卡反欺诈的模型"）
  → Query Embedding
  → Milvus 检索（支持多条件过滤：业务场景、模型类型、性能指标）
  → 返回 Top-K 模型卡片
  → LLM 生成推荐理由
```

**元数据设计：**
```python
{
    "model_id": "M001",
    "model_name": "XGBoost 反欺诈模型 v2.3",
    "scene": "信用卡反欺诈",
    "model_type": "XGBoost",
    "metrics": {"AUC": 0.92, "KS": 0.65},
    "features": ["交易金额", "交易时间", "..."],
    "update_time": "2026-04-15"
}
```

### 3.3 场景三：仿真结果解读（Tool 调用）

**实现方式：**
1. 定义 Tool：`SimulationResultTool`
2. LangGraph Agent 根据用户问题调用 Tool
3. Tool 调用仿真系统 API 获取结果
4. LLM 解读结果并生成自然语言回答

**Tool 定义示例：**
```python
from langchain.tools import Tool

def get_simulation_result(simulation_id: str) -> dict:
    """
    获取仿真结果
    Args:
        simulation_id: 仿真任务 ID
    Returns:
        仿真结果数据（JSON）
    """
    # 调用仿真系统 API
    response = requests.get(f"http://simulation-api/results/{simulation_id}")
    return response.json()

simulation_tool = Tool(
    name="get_simulation_result",
    func=get_simulation_result,
    description="获取指定仿真任务的结果数据"
)
```

**工作流程：**
```python
用户："帮我看看仿真任务 SIM-20260507-001 的结果"
  → Agent 识别需要调用 simulation_tool
  → 调用 Tool 获取结果
  → LLM 分析结果（关键指标、异常点、趋势）
  → 生成解读报告
```

### 3.4 场景四：毛利抽成查询（Tool 调用）

**实现方式：**
1. 定义 Tool：`ProfitQueryTool`
2. 支持多种查询维度（按产品、按时间、按渠道）
3. 直接查询数据库或调用业务 API
4. LLM 格式化结果并生成摘要

**Tool 定义示例：**
```python
def query_profit_commission(
    product_id: str = None,
    start_date: str = None,
    end_date: str = None,
    channel: str = None
) -> dict:
    """
    查询毛利抽成数据
    Args:
        product_id: 产品 ID
        start_date: 开始日期 (YYYY-MM-DD)
        end_date: 结束日期 (YYYY-MM-DD)
        channel: 渠道名称
    Returns:
        毛利抽成数据
    """
    # 构建 SQL 查询或调用 API
    query = build_query(product_id, start_date, end_date, channel)
    result = db.execute(query)
    return result

profit_tool = Tool(
    name="query_profit_commission",
    func=query_profit_commission,
    description="查询产品的毛利和抽成数据，支持按产品、时间、渠道筛选"
)
```

**工作流程：**
```python
用户："查询产品 P001 在 4 月份的毛利情况"
  → Agent 提取参数（product_id=P001, start_date=2026-04-01, end_date=2026-04-30）
  → 调用 profit_tool
  → 获取数据（总毛利、抽成比例、各渠道明细）
  → LLM 生成摘要和可视化建议
```

## 4. 统一路由与编排

### 4.1 意图识别

使用 LangGraph 构建意图识别 Agent，根据用户问题自动路由到对应场景。

**实现方式：**
```python
from langgraph.graph import StateGraph, END

# 定义状态
class AgentState(TypedDict):
    user_query: str
    scene_type: str  # risk_rule / model_card / simulation / profit
    result: dict

# 意图识别节点
def classify_intent(state: AgentState) -> AgentState:
    query = state["user_query"]
    # 使用 LLM 分类（Few-shot Prompt）
    prompt = f"""
    判断以下问题属于哪个场景：
    1. risk_rule - 风控规则相关问题
    2. model_card - 模型查询和推荐
    3. simulation - 仿真结果查询
    4. profit - 毛利抽成查询
    
    问题：{query}
    场景类型：
    """
    scene_type = llm.predict(prompt).strip()
    state["scene_type"] = scene_type
    return state

# 路由函数
def route_to_scene(state: AgentState) -> str:
    return state["scene_type"]

# 构建图
workflow = StateGraph(AgentState)
workflow.add_node("classify", classify_intent)
workflow.add_node("risk_rule_agent", handle_risk_rule)
workflow.add_node("model_card_agent", handle_model_card)
workflow.add_node("simulation_agent", handle_simulation)
workflow.add_node("profit_agent", handle_profit)

workflow.set_entry_point("classify")
workflow.add_conditional_edges(
    "classify",
    route_to_scene,
    {
        "risk_rule": "risk_rule_agent",
        "model_card": "model_card_agent",
        "simulation": "simulation_agent",
        "profit": "profit_agent"
    }
)
```

### 4.2 场景 Agent 实现

每个场景 Agent 负责具体的业务逻辑：

**RAG 场景（风控规则、模型卡片）：**
```python
def handle_risk_rule(state: AgentState) -> AgentState:
    query = state["user_query"]
    
    # 1. 查询改写（可选）
    rewritten_query = query_rewriter.rewrite(query)
    
    # 2. 向量检索
    retriever = MilvusRetriever(
        collection_name="unified_docs",
        embedding_function=bge_m3_embeddings,
        search_kwargs={
            "k": 5,
            "expr": "scene_type == 'risk_rule'"  # 标量过滤
        }
    )
    docs = retriever.get_relevant_documents(rewritten_query)
    
    # 3. Rerank（可选）
    reranked_docs = reranker.rerank(query, docs, top_k=3)
    
    # 4. 构建 Prompt
    context = "\n\n".join([doc.page_content for doc in reranked_docs])
    prompt = f"""
    基于以下风控规则文档回答问题：
    
    {context}
    
    问题：{query}
    
    要求：
    1. 准确引用规则内容
    2. 如果文档中没有相关信息，明确说明
    3. 提供规则来源（文档名称、章节）
    """
    
    # 5. LLM 生成答案
    answer = llm.predict(prompt)
    
    state["result"] = {
        "answer": answer,
        "sources": [doc.metadata for doc in reranked_docs]
    }
    return state
```

**Tool 调用场景（仿真、毛利）：**
```python
def handle_simulation(state: AgentState) -> AgentState:
    query = state["user_query"]
    
    # 1. 使用 LLM + Tool 的 Agent
    tools = [simulation_tool]
    agent = create_react_agent(llm, tools)
    
    # 2. Agent 自动决策是否调用 Tool
    result = agent.invoke({"input": query})
    
    state["result"] = result
    return state
```

## 5. 数据管理

### 5.1 数据摄入流程

**批量导入（离线）：**
```python
# 1. 加载文档
from langchain.document_loaders import DirectoryLoader, PDFLoader

loader = DirectoryLoader(
    "data/risk_rules/",
    glob="**/*.pdf",
    loader_cls=PDFLoader
)
documents = loader.load()

# 2. 文本分块
from langchain.text_splitter import RecursiveCharacterTextSplitter

text_splitter = RecursiveCharacterTextSplitter(
    chunk_size=500,
    chunk_overlap=50,
    separators=["\n\n", "\n", "。", "！", "？", "；", " ", ""]
)
chunks = text_splitter.split_documents(documents)

# 3. 生成 Embedding
embeddings = BGE_M3_Embeddings()
vectors = embeddings.embed_documents([chunk.page_content for chunk in chunks])

# 4. 存入 Milvus
from pymilvus import Collection
collection = Collection("unified_docs")
collection.insert([
    {
        "id": f"risk_rule_{i}",
        "vector": vectors[i],
        "scene_type": "risk_rule",
        "content": chunks[i].page_content,
        "metadata": chunks[i].metadata
    }
    for i in range(len(chunks))
])
```

**增量更新（在线）：**
```python
# 监听数据源变化，增量更新向量库
def incremental_update(doc_id: str, content: str, scene_type: str):
    # 1. 生成 Embedding
    vector = embeddings.embed_query(content)
    
    # 2. 检查是否已存在
    existing = collection.query(expr=f"id == '{doc_id}'")
    
    if existing:
        # 更新
        collection.delete(expr=f"id == '{doc_id}'")
    
    # 插入新数据
    collection.insert([{
        "id": doc_id,
        "vector": vector,
        "scene_type": scene_type,
        "content": content,
        "metadata": {"update_time": datetime.now().isoformat()}
    }])
```

### 5.2 数据版本管理

**策略：**
- 使用 `version` 字段标记数据版本
- 查询时优先返回最新版本
- 保留历史版本用于审计和回溯

```python
# Milvus 查询时过滤最新版本
retriever = MilvusRetriever(
    search_kwargs={
        "k": 5,
        "expr": "scene_type == 'risk_rule' AND is_latest == True"
    }
)
```

## 6. 性能优化

### 6.1 缓存策略

**多层缓存：**
1. **Query 缓存**：相同问题直接返回缓存结果（Redis，TTL=1h）
2. **Embedding 缓存**：常见查询的 Embedding 预计算
3. **LLM 响应缓存**：相似问题的 LLM 回答缓存

**实现示例：**
```python
import hashlib
from functools import lru_cache

class CachedRAGPipeline:
    def __init__(self, redis_client):
        self.redis = redis_client
        self.ttl = 3600  # 1 小时
    
    def query(self, user_query: str, scene_type: str) -> dict:
        # 生成缓存 key
        cache_key = hashlib.md5(
            f"{scene_type}:{user_query}".encode()
        ).hexdigest()
        
        # 检查缓存
        cached = self.redis.get(cache_key)
        if cached:
            return json.loads(cached)
        
        # 执行查询
        result = self._execute_query(user_query, scene_type)
        
        # 写入缓存
        self.redis.setex(cache_key, self.ttl, json.dumps(result))
        return result
```

### 6.2 并发优化

**异步处理：**
```python
import asyncio
from langchain.callbacks import AsyncCallbackHandler

async def async_rag_query(query: str):
    # 并行执行 Embedding 和意图识别
    embedding_task = asyncio.create_task(
        embeddings.aembed_query(query)
    )
    intent_task = asyncio.create_task(
        classify_intent_async(query)
    )
    
    vector, scene_type = await asyncio.gather(
        embedding_task, intent_task
    )
    
    # 后续处理...
```

### 6.3 检索优化

**混合检索权重调优：**
```python
# Milvus 混合检索
search_params = {
    "metric_type": "IP",  # 内积
    "params": {
        "nprobe": 10,
        "dense_weight": 0.7,   # 稠密向量权重
        "sparse_weight": 0.3   # 稀疏向量权重
    }
}
```

**分区策略：**
```python
# 按场景分区，减少检索范围
collection.create_partition("risk_rule")
collection.create_partition("model_card")

# 查询时指定分区
results = collection.search(
    data=[query_vector],
    anns_field="vector",
    param=search_params,
    limit=5,
    partition_names=["risk_rule"]  # 只搜索风控规则分区
)
```

## 7. 可观测性

### 7.1 日志记录

**关键指标：**
- 请求 ID、用户 ID、场景类型
- 意图识别结果
- 检索耗时、LLM 耗时、总耗时
- 检索到的文档数量、相似度分数
- LLM Token 消耗

**实现示例：**
```python
import logging
import time

logger = logging.getLogger("rag_system")

def log_query(request_id, user_query, scene_type, result, metrics):
    logger.info({
        "request_id": request_id,
        "user_query": user_query,
        "scene_type": scene_type,
        "retrieval_time_ms": metrics["retrieval_time"],
        "llm_time_ms": metrics["llm_time"],
        "total_time_ms": metrics["total_time"],
        "num_docs_retrieved": len(result.get("sources", [])),
        "tokens_used": metrics["tokens"]
    })
```

### 7.2 监控指标

**系统指标：**
- QPS（每秒查询数）
- P50/P95/P99 延迟
- 错误率
- 缓存命中率

**业务指标：**
- 各场景查询分布
- 用户满意度（点赞/点踩）
- 检索召回率（人工标注）

**监控工具：**
- **Prometheus + Grafana**：系统指标监控
- **LangSmith**：LangChain 调用链追踪
- **Elastic APM**：分布式追踪

### 7.3 追踪与调试

**LangSmith 集成：**
```python
import os
os.environ["LANGCHAIN_TRACING_V2"] = "true"
os.environ["LANGCHAIN_API_KEY"] = "your_api_key"

# 自动记录所有 LangChain 调用
chain = RetrievalQA.from_chain_type(
    llm=llm,
    retriever=retriever,
    return_source_documents=True
)
```

## 8. 部署方案

### 8.1 架构拓扑

```
┌─────────────────────────────────────────────────────────────┐
│                      负载均衡 (Nginx)                         │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│              API 服务集群 (FastAPI + Gunicorn)               │
│                    (3 个实例，自动扩缩容)                      │
└─────────────────────────────────────────────────────────────┘
                              ↓
        ┌──────────────┬──────────────┬──────────────┐
        ↓              ↓              ↓              ↓
┌──────────────┐ ┌──────────────┐ ┌──────────────┐ ┌──────────────┐
│ Milvus 集群  │ │ Redis 集群   │ │ LLM 服务     │ │ 业务系统 API │
│ (3 节点)     │ │ (主从 + 哨兵)│ │ (OpenAI API) │ │              │
└──────────────┘ └──────────────┘ └──────────────┘ └──────────────┘
```

### 8.2 容器化部署

**Docker Compose 示例：**
```yaml
version: '3.8'

services:
  api:
    build: ./api
    ports:
      - "8000:8000"
    environment:
      - MILVUS_HOST=milvus
      - REDIS_HOST=redis
      - OPENAI_API_KEY=${OPENAI_API_KEY}
    depends_on:
      - milvus
      - redis
    deploy:
      replicas: 3

  milvus:
    image: milvusdb/milvus:v2.3.0
    ports:
      - "19530:19530"
    volumes:
      - milvus_data:/var/lib/milvus

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data

volumes:
  milvus_data:
  redis_data:
```

### 8.3 Kubernetes 部署（生产环境）

**关键配置：**
- HPA（水平自动扩缩容）：根据 CPU/内存/QPS 自动扩容
- PVC（持久化存储）：Milvus 数据持久化
- ConfigMap：配置管理
- Secret：敏感信息管理（API Key）

## 9. 成本估算

### 9.1 基础设施成本（月）

| 组件 | 规格 | 数量 | 单价 | 小计 |
|------|------|------|------|------|
| API 服务器 | 4C8G | 3 | ¥500 | ¥1,500 |
| Milvus 集群 | 8C16G | 3 | ¥1,000 | ¥3,000 |
| Redis 集群 | 4C8G | 2 | ¥600 | ¥1,200 |
| 负载均衡 | - | 1 | ¥200 | ¥200 |
| **合计** | | | | **¥5,900** |

### 9.2 LLM 调用成本（月）

**假设：**
- 日均查询量：10,000 次
- 平均每次查询：输入 1,000 tokens，输出 500 tokens

**GPT-4 Turbo：**
- 输入：$0.01/1K tokens × 1,000 × 10,000 = $100
- 输出：$0.03/1K tokens × 500 × 10,000 = $150
- 月成本：$250 × 30 = **$7,500**（约 ¥52,500）

**优化方案：**
1. 使用 GPT-3.5 Turbo 处理简单查询（成本降低 90%）
2. 启用缓存，减少重复查询（节省 30-50%）
3. 本地部署开源模型（Qwen2.5-72B），成本降至 GPU 租赁费用

### 9.3 总成本（月）

| 方案 | 基础设施 | LLM 调用 | 总计 |
|------|----------|----------|------|
| 全 GPT-4 | ¥5,900 | ¥52,500 | **¥58,400** |
| GPT-4 + GPT-3.5 混合 | ¥5,900 | ¥26,250 | **¥32,150** |
| 本地部署 | ¥5,900 + ¥8,000 (GPU) | ¥0 | **¥13,900** |

## 10. 实施路线图

### 10.1 第一阶段：MVP（2-3 周）

**目标：** 验证技术可行性，上线单个场景

**任务：**
1. 搭建基础设施（Milvus + Redis + API 服务）
2. 实现风控规则问答场景（RAG）
3. 数据准备：导入 100+ 条风控规则文档
4. 基础监控和日志

**交付物：**
- 可运行的 Demo
- 技术文档
- 性能测试报告

### 10.2 第二阶段：多场景集成（3-4 周）

**目标：** 完成四个场景的统一架构

**任务：**
1. 实现模型卡片检索（RAG）
2. 实现仿真结果解读（Tool 调用）
3. 实现毛利抽成查询（Tool 调用）
4. 统一路由与编排（LangGraph）
5. 缓存优化

**交付物：**
- 完整的四场景系统
- API 文档
- 用户手册

### 10.3 第三阶段：优化与上线（2-3 周）

**目标：** 性能优化，生产环境部署

**任务：**
1. 性能压测与优化（目标：P95 < 2s）
2. 混合检索调优
3. 监控告警配置
4. 灰度发布
5. 用户培训

**交付物：**
- 生产环境部署
- 运维手册
- 监控大盘

## 11. 风险与挑战

### 11.1 技术风险

| 风险 | 影响 | 应对措施 |
|------|------|----------|
| LLM 幻觉问题 | 生成错误答案 | 1. 严格的 Prompt 工程<br>2. 引用溯源机制<br>3. 人工审核高风险场景 |
| 检索召回率低 | 找不到相关文档 | 1. 混合检索（稠密+稀疏）<br>2. 查询改写<br>3. 持续优化 Embedding 模型 |
| 向量库性能瓶颈 | 高并发下响应慢 | 1. 分区策略<br>2. 缓存优化<br>3. 集群扩容 |
| LLM API 限流 | 服务不可用 | 1. 多 API Key 轮询<br>2. 本地模型备份<br>3. 降级策略 |

### 11.2 业务风险

| 风险 | 影响 | 应对措施 |
|------|------|----------|
| 数据质量差 | 检索结果不准确 | 1. 数据清洗流程<br>2. 人工标注<br>3. 持续迭代 |
| 用户接受度低 | 使用率低 | 1. 用户培训<br>2. 收集反馈<br>3. 快速迭代 |
| 成本超预算 | 项目暂停 | 1. 成本监控<br>2. 本地模型方案<br>3. 缓存优化 |

## 12. 后续优化方向

### 12.1 短期优化（1-3 个月）

1. **多轮对话支持**：记忆上下文，支持追问
2. **个性化推荐**：根据用户历史查询优化结果
3. **多模态支持**：支持图表、表格的解读
4. **A/B 测试**：对比不同检索策略的效果

### 12.2 长期规划（3-12 个月）

1. **Fine-tuning**：基于业务数据微调 Embedding 模型和 LLM
2. **知识图谱集成**：结合知识图谱增强推理能力
3. **主动学习**：从用户反馈中持续学习
4. **多语言支持**：支持英文、日文等其他语言

## 13. 总结

本方案通过统一的技术架构，整合了四个业务场景，具有以下优势：

✅ **技术统一**：共用基础设施，降低运维成本  
✅ **灵活扩展**：易于新增场景和数据源  
✅ **性能优秀**：多层缓存 + 混合检索，P95 < 2s  
✅ **成本可控**：支持多种 LLM 方案，灵活调整  
✅ **可观测性强**：完整的监控和追踪体系  

**推荐实施路径：**
1. 先上线 MVP（风控规则问答），验证技术可行性
2. 逐步集成其他场景，完善统一架构
3. 持续优化性能和成本，收集用户反馈

---

**文档版本：** v1.0  
**最后更新：** 2026-05-07  
**负责人：** [待填写]

---

## 附录 A：gRPC 实现方案

### A.1 Proto 文件定义

```protobuf
// rag_service.proto
syntax = "proto3";

package rag;

service RagService {
  rpc Query(QueryRequest) returns (QueryResponse);
  rpc StreamQuery(QueryRequest) returns (stream QueryResponse);  // 流式返回
}

message QueryRequest {
  string query = 1;
  string scene_type = 2;
  string user_id = 3;
}

message QueryResponse {
  string answer = 1;
  repeated Source sources = 2;
  string scene_type = 3;
  string request_id = 4;
}

message Source {
  string doc_id = 1;
  string content = 2;
  float score = 3;
}
```

### A.2 Python 服务端

```python
import grpc
from concurrent import futures
import rag_service_pb2
import rag_service_pb2_grpc

class RagServicer(rag_service_pb2_grpc.RagServiceServicer):
    
    async def Query(self, request, context):
        result = await rag_pipeline.process(
            query=request.query,
            scene_type=request.scene_type,
            user_id=request.user_id
        )
        
        return rag_service_pb2.QueryResponse(
            answer=result['answer'],
            sources=[
                rag_service_pb2.Source(**src) 
                for src in result['sources']
            ],
            scene_type=result['scene_type'],
            request_id=result['request_id']
        )
    
    async def StreamQuery(self, request, context):
        # 流式返回（适合 LLM 逐字输出）
        async for chunk in rag_pipeline.stream_process(request.query):
            yield rag_service_pb2.QueryResponse(
                answer=chunk,
                scene_type=request.scene_type
            )

# 启动服务
server = grpc.aio.server(futures.ThreadPoolExecutor(max_workers=10))
rag_service_pb2_grpc.add_RagServiceServicer_to_server(
    RagServicer(), server
)
server.add_insecure_port('[::]:50051')
await server.start()
```

### A.3 Java 客户端

```java
@Service
public class RagGrpcClient {
    
    private final RagServiceGrpc.RagServiceBlockingStub blockingStub;
    
    public RagGrpcClient() {
        ManagedChannel channel = ManagedChannelBuilder
            .forAddress("rag-service", 50051)
            .usePlaintext()
            .build();
        
        this.blockingStub = RagServiceGrpc.newBlockingStub(channel);
    }
    
    public QueryResponse query(String query, String sceneType) {
        QueryRequest request = QueryRequest.newBuilder()
            .setQuery(query)
            .setSceneType(sceneType)
            .build();
        
        return blockingStub.query(request);
    }
    
    // 流式调用
    public void streamQuery(String query, Consumer<String> callback) {
        QueryRequest request = QueryRequest.newBuilder()
            .setQuery(query)
            .build();
        
        Iterator<QueryResponse> responses = blockingStub.streamQuery(request);
        while (responses.hasNext()) {
            callback.accept(responses.next().getAnswer());
        }
    }
}
```

### A.4 性能对比

| 指标 | HTTP REST | gRPC | 提升 |
|------|-----------|------|------|
| 序列化速度 | JSON（慢） | Protobuf（快） | 5-10x |
| 传输大小 | 大 | 小（二进制） | 30-50% |
| 延迟 | ~50ms | ~10ms | 5x |
| 流式支持 | SSE（复杂） | 原生支持 | ✅ |

**建议：**
- MVP 阶段使用 HTTP REST（快速验证）
- 生产环境高并发场景切换到 gRPC

---

## 附录 B：架构调整方案（基于评审结果）

### B.1 核心调整：取消统一意图识别，改为直接路由

**原方案问题：**
- 所有查询都需要 LLM 意图识别，增加延迟和成本
- 意图识别可能出错，影响用户体验
- 如果上游系统已知场景，意图识别是多余的

**调整后方案：**
- 每个场景独立 API endpoint
- 上游系统直接调用对应 endpoint，无需意图识别
- 可选提供统一入口（适用于对话式 UI）

### B.2 LangGraph 使用场景调整

**不再用于：** 跨场景路由（改用简单的 API 路由）

**重点用于：** 场景内部的复杂交互
- ✅ 多轮对话（记忆上下文，支持追问）
- ✅ 自动钻取（发现异常自动深入分析）
- ✅ 并行执行（同时调用多个数据源）
- ✅ 人工介入（高风险操作需确认）

### B.3 调整后的架构图

```
Java 上游系统
  ↓
┌─────────────────────────────────────────────────────────────┐
│                    FastAPI (直接路由)                         │
│  POST /api/v1/risk-rules/query                              │
│  POST /api/v1/model-cards/query                             │
│  POST /api/v1/simulation/query                              │
│  POST /api/v1/profit/query                                  │
└─────────────────────────────────────────────────────────────┘
  ↓           ↓              ↓            ↓
┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐
│风控规则  │ │模型卡片  │ │仿真解读  │ │毛利查询  │
│Agent     │ │Agent     │ │Agent     │ │Agent     │
│(LangGraph)│ │(LangGraph)│ │(LangGraph)│ │(LangGraph)│
└──────────┘ └──────────┘ └──────────┘ └──────────┘
  ↓           ↓              ↓            ↓
┌─────────────────────────────────────────────────────────────┐
│              状态管理层 (Redis + Checkpointer)                │
└─────────────────────────────────────────────────────────────┘
  ↓           ↓              ↓            ↓
┌─────────────────────────────────────────────────────────────┐
│  Milvus    │  LLM       │  仿真API   │  业务DB              │
└─────────────────────────────────────────────────────────────┘
```

---

## 附录 C：分阶段实施计划

### C.1 实施原则

- **渐进式交付**：先简单后复杂，快速验证核心价值
- **数据驱动**：边用边补充数据，持续优化
- **风险可控**：每个阶段都有明确的验收标准和回滚方案

### C.2 阶段划分

#### 阶段 0：数据准备 + 基础设施（1-2周）

**目标：** 搭建基础环境，准备第一批数据

**技术任务：**
- [ ] 部署 Milvus 向量数据库
- [ ] 部署 Redis（会话状态存储）
- [ ] 搭建 FastAPI 服务框架
- [ ] 实现数据摄入 Pipeline（Document Loader + Chunking + Embedding）
- [ ] 前端开发会话管理组件

**数据任务：**
- [ ] 收集 20-50 条风控规则文档（Markdown/PDF）
- [ ] 整理文档结构和元数据
- [ ] 标注 10 个典型问答对（用于测试）

**交付物：**
- 可运行的基础设施环境
- 数据摄入脚本和文档
- 风控规则向量库（已导入数据）

#### 阶段 1：MVP - 单场景基础问答（2-3周）

**目标：** 上线风控规则问答，验证核心技术路线

**能力范围：**
- ✅ 单轮问答（暂不支持多轮对话）
- ✅ 基础 RAG 检索（向量检索 + LLM 生成）
- ✅ 引用溯源（返回来源文档）
- ❌ 暂不支持：多轮对话、自动钻取、并行执行、人工介入

**技术实现：**
- 使用 LangChain RetrievalQA（不用 LangGraph）
- 单次请求-响应模式
- 基础监控和日志

**验收标准：**
- [ ] 能正确回答 10 个标注问题中的 8 个（80% 准确率）
- [ ] P95 响应时间 < 3s
- [ ] 返回答案附带引用来源（文档名称、章节）

**前端需求：**
- 单次问答界面（输入框 + 答案展示）
- 展示来源文档链接

#### 阶段 2：增强 - 多轮对话 + 会话管理（2周）

**目标：** 支持用户追问和上下文记忆

**新增能力：**
- ✅ 多轮对话（记忆上下文，支持追问）
- ✅ 会话管理（Redis 存储，30分钟过期）
- ✅ 澄清机制（检测到歧义时主动询问）
- ❌ 暂不支持：自动钻取、并行执行、人工介入

**技术升级：**
- 引入 LangGraph + RedisSaver（Checkpointer）
- 实现会话状态持久化
- 增加意图澄清节点

**验收标准：**
- [ ] 支持 3 轮以上连续对话
- [ ] 会话状态正确保存和恢复
- [ ] 能识别歧义并主动澄清（如"白金卡还是金卡？"）

**前端需求：**
- 对话式界面（类似聊天窗口）
- 保存 session_id，支持刷新页面后继续对话
- 展示对话历史

#### 阶段 3：智能化 - 自动钻取 + 并行执行（2-3周）

**目标：** Agent 能自主决策，提升分析深度

**新增能力：**
- ✅ 自动钻取（发现异常自动深入分析）
- ✅ 并行执行（同时调用多个数据源）
- ✅ 新增场景：模型卡片检索、仿真解读、毛利查询
- ❌ 暂不支持：人工介入

**适用场景：**
- 毛利查询：自动钻取异常维度（总体 → 产品 → 渠道）
- 仿真解读：并行获取多个仿真结果对比
- 模型卡片：并行检索（向量 + 关键词 + 数据库）

**验收标准：**
- [ ] 毛利异常能自动钻取到根因（最多3层）
- [ ] 仿真对比能并行获取 3+ 个结果
- [ ] 模型卡片检索支持多条件筛选

**前端需求：**
- 展示钻取路径（面包屑导航）
- 展示并行任务进度条
- 支持对比视图（表格/图表）

#### 阶段 4：完整版 - 人工介入 + 全场景上线（2周）

**目标：** 完整的 4 场景系统，支持高风险操作确认

**新增能力：**
- ✅ 人工介入（高风险操作需用户确认）
- ✅ 4 个场景全部上线并优化

**人工介入场景示例：**
- 修改生产环境配置
- 批量操作（如批量调整限额）
- 敏感数据查询

**验收标准：**
- [ ] 4 个场景全部上线
- [ ] 高风险操作能正确触发人工确认
- [ ] 用户拒绝后能正确回滚或取消

**前端需求：**
- 确认弹窗（展示风险等级、操作详情、影响范围）
- 支持批量确认
- 操作日志记录

#### 阶段 5：优化与监控（持续）

**目标：** 性能优化、成本控制、可观测性

**任务清单：**
- [ ] 缓存策略（Query 缓存、Embedding 缓存）
- [ ] 混合检索权重调优
- [ ] LLM 成本优化（简单查询用 GPT-3.5）
- [ ] LangSmith 集成（调用链追踪）
- [ ] Prometheus + Grafana（系统指标）
- [ ] 用户反馈收集（点赞/点踩）
- [ ] 根据反馈补充文档和优化 Prompt

### C.3 时间线总览

```
Week 1-2:   阶段 0 (数据准备 + 基础设施)
Week 3-5:   阶段 1 (MVP 单场景)
Week 6-7:   阶段 2 (多轮对话)
Week 8-10:  阶段 3 (自动钻取 + 并行 + 多场景)
Week 11-12: 阶段 4 (人工介入)
Week 13+:   阶段 5 (持续优化)
```

**总计：** 约 3 个月完整上线

### C.4 各阶段数据需求

| 阶段 | 数据需求 | 数量 | 优先级 |
|------|----------|------|--------|
| 阶段 0-1 | 风控规则文档 | 20-50 条 | P0 |
| 阶段 2 | 补充规则 + 标注多轮对话 | +50 条 + 20 组对话 | P1 |
| 阶段 3 | 模型卡片 + 仿真API + 毛利Schema | 100+ 模型 + API文档 + Schema | P1 |
| 阶段 4 | 业务规则 + 风险等级定义 | 完整业务规则文档 | P2 |

### C.5 风险与依赖

| 风险 | 影响阶段 | 缓解措施 |
|------|----------|----------|
| 数据质量差 | 阶段 1 | 先用小数据集验证，边用边补充 |
| 前端开发延期 | 阶段 2 | 先用 Postman 测试后端，前端并行开发 |
| LLM 成本超预算 | 阶段 3+ | 简单查询用 GPT-3.5，启用缓存 |
| 仿真 API 不稳定 | 阶段 3 | 增加重试机制，降级到返回原始数据 |
| 数据准备延期 | 所有阶段 | 技术侧先搭建环境，数据侧并行准备 |

---

## 附录 D：数据格式规范

### D.1 风控规则文档格式

**推荐格式：Markdown（最佳）**

```markdown
---
metadata:
  rule_id: "R001"
  rule_name: "信用卡单笔交易限额规则"
  category: "交易限额"
  product: "信用卡"
  version: "v2.3"
  update_date: "2026-04-15"
  status: "生效中"
---

# 信用卡单笔交易限额规则

## 规则概述
本规则定义了不同等级信用卡的单笔交易限额。

## 适用范围
- 产品类型：个人信用卡
- 卡片等级：白金卡、金卡、普卡

## 规则详情

### 白金卡
- 单笔限额：50,000 元
- 日累计限额：200,000 元

### 金卡
- 单笔限额：20,000 元
- 日累计限额：80,000 元

## 生效时间
2026-04-15 起生效
```

**备选格式：PDF、Word**
- 确保有清晰的标题层级
- 避免扫描件
- 文件命名：`{rule_id}_{rule_name}.pdf`

### D.2 模型卡片数据格式

**推荐格式：JSON**

```json
{
  "model_id": "M001",
  "model_name": "XGBoost 反欺诈模型 v2.3",
  "scene": "信用卡反欺诈",
  "model_type": "XGBoost",
  "metrics": {
    "AUC": 0.92,
    "KS": 0.65
  },
  "status": "生产环境",
  "update_date": "2026-04-15"
}
```

**备选格式：CSV**（简化版）

---

**文档版本：** v2.0  
**最后更新：** 2026-05-07  
**负责人：** [待填写]

